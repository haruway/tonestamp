# macOS
.DS_Store

# Node (only used for the build script and optional dev tooling)
node_modules/
npm-debug.log*

# Editors
.vscode/
.idea/

# Local scratch
*.local
tmp/

# Material de trabalho que não é do público: briefing, protótipo antigo,
# rascunhos. Mora em ../_original/, mas fica bloqueado aqui também caso
# alguém copie pra dentro do repo sem pensar.
_original/
PRD*.md
*-tool.html

# NOTE: dist/ is intentionally NOT ignored.
# The single-file build is versioned so anyone can download and run it.
